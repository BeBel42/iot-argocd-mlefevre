apiVersion: app/v1
kind: Deployment
metadate:
  name: wil-playground
  label:
    app: main
spec:
  replicas: 1
  selector:
    matchLabels:
      app: main
    template:
      metadata:
        labels:
          app: main
      spec:
        containers:
        - name: wil-playground
          image: wil42/playground:v1
          ports:
            containerPort: 8888
